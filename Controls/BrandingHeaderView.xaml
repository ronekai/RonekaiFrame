<UserControl x:Class="RonekaiImageFramer.Controls.BrandingHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Image x:Name="BrandLogoImage" MaxHeight="52" MaxWidth="180" Margin="0,0,10,0"
               Visibility="Collapsed" Stretch="Uniform"/>
        <StackPanel VerticalAlignment="Center">
            <StackPanel x:Name="BrandTitleRow" Orientation="Horizontal">
                <TextBlock x:Name="BrandMainText" Text="RONEKAI" FontSize="26" FontWeight="Bold"
                           Foreground="{StaticResource BrandPrimaryBrush}"/>
                <TextBlock x:Name="BrandSuffixText" Text=".DEN" FontSize="13"
                           Foreground="{StaticResource BrandAccentBrush}"
                           VerticalAlignment="Bottom" Margin="2,0,0,3"/>
            </StackPanel>
            <TextBlock x:Name="BrandTaglineText"
                       Text="PhonixFrame — toplu e-ticaret görsel stüdyo"
                       FontSize="12" Foreground="{StaticResource BrandMutedBrush}"
                       Margin="0,2,0,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
